# OmniFlow 当前项目优化汇总

更新时间：2026-06-10  
项目路径：`C:\Users\Administrator\Documents\品牌集成产品开发\omiflow-current`

## 一、当前项目定位

OmniFlow 当前版本是一套面向企业多平台经营管理的集成运营中控原型，覆盖：

- 多平台经营数据看板
- Excel / CSV 数据上传与分析
- 数字化 Agent 工作室
- 供应链、财务、客服和人事业务模块
- 配置与任务中心
- AI 工作流调用和本地降级输出
- 内部访问认证与接口保护

## 二、已经完成的核心优化

### 1. 大表格分析稳定性

- 重构 `profileTable()` 的统计方式，避免对大数组使用容易触发栈溢出的操作。
- 移除原先超过 5000 行就跳过分析的临时限制。
- 表格 profile 可以随 sheet 缓存和复用，减少重复计算。
- 宽表日序列提取增加数量上限，避免生成过多中间数据。
- `ExtractGroupedDailySeries` 相关处理限制为最多 5000 个数据点，并限制锚点和列扫描数量。

主要文件：

- `src/utils/tableAnalysis.ts`
- `src/components/ImportMappingModal.tsx`
- `src/components/TableParsedBoardTab.tsx`

### 2. Chrome 翻译导致 React 崩溃

- 页面根节点及关键图表区域增加 `notranslate` 保护。
- 调整 Recharts 图表尺寸计算，减少翻译插件改写 SVG 文本节点后触发 `removeChild` 异常的概率。
- 避免关键图表依赖容易被外部 DOM 修改干扰的容器结构。

主要文件：

- `index.html`
- `src/main.tsx`
- `src/components/TableParsedBoardTab.tsx`

### 3. AI 服务层统一

- 新增统一 AI 接口文件 `ai-service.ts`。
- 服务端所有模型调用统一通过 `generateAI()` 执行。
- 支持 DeepSeek、通义千问、Moonshot、GLM 和 OpenAI-compatible API。
- Provider、模型和 API Key 由环境变量控制。
- 已包含客户端缓存、主备 Provider、重试和限流熔断逻辑。
- 已移除服务端对 `@google/genai`、`GoogleGenAI` 和 Gemini 模型常量的直接依赖。
- DeepSeek 外部接口和项目内部工作流链路均已验证成功。

主要文件：

- `ai-service.ts`
- `server.ts`
- `.env.example`
- `package.json`
- `package-lock.json`

### 4. Agent 工作室交互修复

- 点击“立即执行 Agent 提质与生产”后，页面会保持在“数字化 Agent 工作室”。
- 执行按钮已设置为 `type="button"`，防止默认表单行为造成页面状态重置。
- 执行开始、成功、接口失败和网络异常时都会显式保持 `activeTab = "workflows"`。
- `/api/gemini/run-workflow` 返回的 `result` 会稳定显示在右侧结果面板。
- 接口返回 200 但 `result` 为空时，会显示明确提示。
- 接口非 200 时，会在当前工作室内显示状态码和失败说明。
- 工作日志仍会刷新，但 `fetchWorkflowLogs()` 不再影响当前主视图。
- 已通过浏览器验证：DeepSeek 输出、SUCCESS 状态和 Completed 日志都能正常显示。

主要文件：

- `src/App.tsx`

### 5. 生产启动与静态构建

- 修复 `node dist/server.cjs` 在未设置 `NODE_ENV=production` 时仍加载 Vite HMR 的问题。
- `npm run start` 或直接运行 `dist/server.cjs` 时，现在会自动服务 `dist` 静态构建。
- `npm run dev` / `tsx server.ts` 仍保留 Vite 开发中间件。
- 避免 Vite HMR 重载导致 React 状态回到默认 dashboard。

主要文件：

- `server.ts`

### 6. 认证和接口安全

- 增加基于签名 HttpOnly Cookie 的浏览器会话。
- 增加登录、退出和会话状态接口。
- 增加 API 访问保护和请求 Origin 校验。
- 浏览器端不再从 localStorage 读取管理 Token。
- 支持临时 Basic Auth、服务端管理 Token 和生产环境强制认证策略。

主要文件：

- `server/auth.ts`
- `server.ts`
- `src/App.tsx`
- `src/utils/adminAuth.ts`
- `.env.example`

### 7. 配置与任务中心并入

- 增加品牌主数据、平台连接、AI 工作流规则、导入数据和任务池接口。
- 增加“配置 / 任务中心”前端页面。
- 支持任务状态更新和规则试跑。
- 旧项目数据可在首次访问时初始化到当前项目 `.data` 目录。

主要文件：

- `server/opsIntegration.ts`
- `src/components/OperationsIntegrationTab.tsx`
- `src/App.tsx`
- `.data/*.json`

## 三、当前运行方式

安装依赖：

```powershell
cd "C:\Users\Administrator\Documents\品牌集成产品开发\omiflow-current"
npm install
```

开发模式：

```powershell
npm run dev
```

生产构建和启动：

```powershell
npm run lint
npm run build
$env:PORT="3001"
npm run start
```

访问地址：

`http://127.0.0.1:3001/`

## 四、已经完成的验证

- `npm run lint` 通过。
- `npm run build` 通过。
- 3001 端口生产服务可访问。
- 首页使用 `/assets/index-*.js` 静态产物，不再加载 `/@vite/client`。
- `/api/health` 返回正常。
- DeepSeek 请求成功并进入 `generateAI()`。
- `/api/gemini/run-workflow` 返回 200。
- Agent 执行后保持在工作室。
- Agent 输出在右侧结果面板可见。
- 日志区显示执行完成状态。
- 未发现当前页面框架错误覆盖层。

## 五、当前限制和风险

1. 前端主 bundle 约 2.18 MB，构建仍有超过 500 KB 的 warning，后续应按业务模块进行动态加载和代码分包。
2. `.data` 使用 JSON 文件持久化，适合当前原型，不适合生产多用户并发写入。
3. 部分历史源码存在文本编码遗留问题，PowerShell 查看时可能显示乱码，但浏览器构建内容基本正常。
4. 当前真实 AI 调用已验证，但尚未建立自动化 AI 回归测试和结果质量评估集。
5. 当前浏览器验证以桌面视口为主，移动端和不同 Chrome 扩展组合还需补充回归。
6. `server.ts` 仍然较大，后续可以按业务路由逐步拆分，但不建议一次性重写。
7. 依赖审计此前提示存在高风险依赖项，需要单独运行 `npm audit` 后评估兼容性再升级。

## 六、建议下一步

1. 为 Agent 工作室增加 Playwright 自动化测试，覆盖成功、空结果、非 200 和网络失败四种状态。
2. 将经营看板、表格分析和 Agent 工作室拆成动态加载模块，降低首屏 bundle。
3. 将 JSON 持久化迁移到 PostgreSQL，并为品牌、连接、规则、任务和执行日志定义正式数据表。
4. 建立工作流运行记录，保存输入、Prompt 版本、Provider、模型、耗时、结果和人工审核状态。
5. 增加角色权限、审计日志和生产环境密钥管理。
6. 在核心链路稳定后，再逐步接入 ERP、CRM、电商平台和企业协同系统。

## 七、结论

当前版本已经从单纯的演示看板，推进到可以上传数据、执行 AI Agent、展示结果、记录任务并提供基础安全保护的可运行中控原型。当前最重要的后续工作不是重新接入模型，而是加强数据持久化、权限、工作流审计、自动化测试和生产系统连接能力。
